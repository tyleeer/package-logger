"# package-logger"

### Example

<!-- สาธิตวิธีการใช้ package โดยด้านล่างเป็นเสมือนเป็นไฟล์ js -->

```js
const logger = log();

logger.success("Yes!!");
logger.warn("It's okay!!");
logger.error("No!!");
```
